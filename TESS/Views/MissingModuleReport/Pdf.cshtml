@{
    Layout = "~/Views/Shared/_ReportLayout.cshtml";
    bool showDesc = false;
    if (Request.Params["showDesc"] != null) {
        if(Request.Params["showDesc"] == "1"){
            showDesc = true;
        }
    }
    string oldModule = "";
}

<div class="crm-print-block">
    @foreach (var module in ViewBag.Modules)
    {
        if (module.Customer != oldModule)
        {
            oldModule = module.Customer;
            <h2>@module.Customer</h2>

            <table class="table table-striped table-bordered contract-supervision-table">
                <th style="width: 10em">Article number</th>
                <th>Module</th>
                <th style="width: 20em">System</th>
                <th>Classification</th>
                @foreach (TietoCRM.Models.MissingModuleReportRow row in ViewBag.Modules)
                {
                    if (row.Customer == module.Customer)
                    {
                        <tr class="" style="page-break-inside:avoid">
                            <td>@row.Article_number</td>
                            <td>@row.Module</td>
                            <td>@row.System</td>
                            <td>@row.Classification</td>
                        </tr>
                    }
                }
            </table>
        }
    }
</div>


