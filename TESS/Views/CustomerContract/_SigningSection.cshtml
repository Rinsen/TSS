@{
    Layout = null;
}
<table>
    <tr>
        <td class="crm-table-title">Datum</td>
        <td class="crm-table-title">Datum</td>
    </tr>
    <tr>
        <td class="crm-empty-signing-field">
        </td>
        <td class="crm-empty-signing-field">
        </td>
    </tr>
    <tr>
        <td class="crm-table-title">Kundens underskrift</td>
        <td class="crm-table-title">Leverantörens underskrift</td>
    </tr>
    <tr>
        <td class="crm-empty-signing-field">
            <span class="signing-line"></span>
        </td>
        <td class="crm-empty-signing-field">
            <span class="signing-line"></span>
        </td>
    </tr>
    <tr>
        <td class="crm-table-title">Namnförtydligande</td>
        <td class="crm-table-title">Namnförtydligande</td>
    </tr>
    <tr>
        <td class="crm-empty-signing-field">
            <span class="signing-line" style="font-size: 80%;">@ViewBag.ContractHead.Customer_sign</span> @*Borttaget MN 180705 - Åter 181204*@
            @*<span class="signing-line" style="font-size: 80%;"></span>*@
        </td>
        <td class="crm-empty-signing-field">
            <span class="signing-line" style="font-size: 80%;">@ViewBag.ContractHead.Our_sign</span>
        </td>
    </tr>
</table>
@*<script>
    String.prototype.capitalizeFirstLetter = function() {
        return this.charAt(0).toUpperCase() + this.slice(1).toLowerCase;
    }

    $(document).ready(function () {
        $custCity = $("#custCity");
        var city = decodeURI("@ViewBag.ContractHead.City");
        //city = city.charAt(0).toUpperCase() + city.slice(1).toLowerCase();
        $custCity.html(city + " den");

        $ourCity = $("#ourCity");
        var ourcity = "@ViewBag.Representative.City";
        var iSplit = new Number;
        for (var i = 0, len = ourcity.length; i < len; i++) {
            if (ourcity[i] != " ") {
                if (isNaN(ourcity[i])) {
                    iSplit = i;
                    { break;}
                }
            }
        }
        $ourCity.html(ourcity.slice(iSplit) + " den");
    });
</script>*@